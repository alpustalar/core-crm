/** Instagram (Meta Graph / Messenger Platform) sürüm + base URL — WhatsApp ile aynı host. */
export const INSTAGRAM_GRAPH_API_VERSION = 'v20.0';
export const INSTAGRAM_GRAPH_API_BASE = `https://graph.facebook.com/${INSTAGRAM_GRAPH_API_VERSION}`;

/** Graph API çağrıları için istek zaman aşımı. */
export const INSTAGRAM_SEND_TIMEOUT_MS = 15_000;
