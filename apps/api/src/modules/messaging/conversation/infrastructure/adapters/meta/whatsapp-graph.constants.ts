/** WhatsApp Cloud API (Meta Graph API) sürüm + base URL. meta-ads ile aynı host. */
export const WHATSAPP_GRAPH_API_VERSION = 'v20.0';
export const WHATSAPP_GRAPH_API_BASE = `https://graph.facebook.com/${WHATSAPP_GRAPH_API_VERSION}`;

/** Graph API çağrıları için istek zaman aşımı. */
export const WHATSAPP_SEND_TIMEOUT_MS = 15_000;
