import { InitializeChartOfAccountsHandler } from './initialize-chart-of-accounts.handler';

/**
 * @deprecated logic e2e kontrol edildi testler yazılacak.
 * */

describe('InitializeChartOfAccountsHandler', () => {
  let handler: InitializeChartOfAccountsHandler;

  it.todo(
    'E2E: Should initialize chart of accounts successfully when clinic is new'
  );
});
